# Server environment variables
# Copy this file to .env and adjust values as needed.

PORT=4000
CLIENT_URL=http://localhost:5173
# Set to "mongo" to use a MongoDB instance via Mongoose or "memory" for local development seeding
DATA_BACKEND=mongo
SESSION_TTL_MINUTES=5
DEFAULT_CLIENT_ID=client-1
JWT_SECRET=replace-me

# MongoDB connection string used when DATA_BACKEND=mongo
MONGODB_URI=mongodb+srv://raphael:ralph@nodeexpressprojecttaskm.mzoibkt.mongodb.net/MAKAYLA?retryWrites=true&w=majority
